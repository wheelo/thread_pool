threadpool todo:

( ) Wrapper functions for all pthreads, sem calls: cleaner code, easier to write (allowed to use csapp.c? [from book])
(/) TLS
( ) leapfrogging/stealing
( ) future semaphore
( ) gsq condition variable
    ( ) always call pthread_cond_wait() in a while loop:
        spurious wakeups:
        https://piazza.com/class/hz79cl74dfv3pf?cid=
        also https://piazza.com/class/hz79cl74dfv3pf?cid=184
    ( ) pthread_cond_wait
    ( ) pthread_cond_signal or pthread_cond_broadcast
( ) future_get()
( ) running driver: https://piazza.com/class/hz79cl74dfv3pf?cid=185


( ) add -g to makefile
( ) add _lib.* files to makefile (still runs?)